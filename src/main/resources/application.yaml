spring:
  application:
    name: Notificacion-Service
  RabbitMQ:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 9003

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/